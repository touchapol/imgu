# Maximum file upload size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# Maximum file upload size in MB (for error messages)
MAX_FILE_SIZE_MB=10

# Base URL for generated image links (optional, defaults to request origin)
BASE_URL=https://your-domain.com
